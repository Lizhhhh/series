<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Document</title>
  </head>
  <body>
    <!-- 标准语法 -->
    {{if age > 18}}
    年龄大于18
    {{else if age < 15}}
    年龄小于15
    {{else}}
    年龄不符合要求
    {{/if}}

    <!-- 原始语法 -->
    <% if(age > 18) {%> 年龄大于18 <%} else if (age < 15) {%> 年龄小于15 <%}
    else {%> 年龄不符合要求 <%} %>
  </body>
</html>
